{% extends 'base.html.twig' %}

{% block title %}Bon index{% endblock %}

{% block body %}
    <h1>Bon index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Percent</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for bon in bons %}
            <tr>
                <td>{{ bon.id }}</td>
                <td>{{ bon.code }}</td>
                <td>{{ bon.percent }}</td>
                <td>
                    <a href="{{ path('admin_bon_show', {'id': bon.id}) }}">show</a>
                    <a href="{{ path('admin_bon_edit', {'id': bon.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_bon_new') }}">Create new</a>
{% endblock %}
